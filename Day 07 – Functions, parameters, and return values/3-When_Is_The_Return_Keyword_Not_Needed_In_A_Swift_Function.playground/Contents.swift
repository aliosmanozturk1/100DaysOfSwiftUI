import Cocoa

/// # When is the return keyword not needed in a Swift function?
/// # Bir Swift fonksiyonunda return anahtar sözcüğü ne zaman gerekli değildir?

// BUNU BİTİRMEDİM TEKRAR BAK. 9 ŞUBATTA BUNA ÇALIŞIRKEN PEKTE İYİ ANLAYAMADIM.


/**
 We use the return keyword to send back values from functions in Swift, but there is one specific case where it isn’t needed: when our function contains only a single expression.
 Swift'te fonksiyonlardan değerleri geri göndermek için return anahtar sözcüğünü kullanırız, ancak buna ihtiyaç duyulmayan özel bir durum vardır: fonksiyonumuz yalnızca tek bir ifade içerdiğinde.
 
 Now, “expression” isn’t a word I use often, but it’s important to understand here. When we write programs we do things like this:
 Şimdi, "expression" sık kullandığım bir kelime değildir, ancak burada anlaşılması önemlidir. Program yazarken şöyle şeyler yaparız:



*/
